/target
**/*.rs.bk
*.pdb

.DS_Store

# Coverage reports, the directory itself stays so editors always have a target
/coverage/*
!/coverage/.gitkeep

# Personal ledger journals must never be committed
*.ledger
*.journal

# The ledger fixtures are generated on demand from the committed manifest
/data/fixtures/
